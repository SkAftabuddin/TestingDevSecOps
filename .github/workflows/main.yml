name: Ephemeral Test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Create Temporary File
        run: echo "secret-data" > temp.txt

      - name: Verify File Exists
        run: cat temp.txt
